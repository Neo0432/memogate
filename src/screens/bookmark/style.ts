import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 4,
    marginTop: 12,
    paddingInline: 16,
    gap: 16,
  },

  scrollViewContainer: {
    gap: 16,
  },
});
