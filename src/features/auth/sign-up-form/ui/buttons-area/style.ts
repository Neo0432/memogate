import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonArea: {
    alignItems: 'center',
    gap: 12,
  },

  submitButton: {
    width: '100%',
  },

  textUnderButton: {
    display: 'flex',
    flexDirection: 'row',
    gap: 8,
  },
});
